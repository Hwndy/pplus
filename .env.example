# Base URL of the P+ API, including the version prefix.
# Local backend: http://localhost:4000/api/v1
VITE_API_BASE_URL=

# Display name used in the browser title fallback.
VITE_APP_NAME=P+ Media Analytics
